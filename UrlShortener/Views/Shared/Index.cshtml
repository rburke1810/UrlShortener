@page
<div class="jumbotron">
    <div class="row">
        <div class="col-lg-12 col-lg-offset-2">
            <div>
                <pre>
Below is a tool to shorten long URL's.
With twitter taking off and a character limit on your tweets, 
here's a great way to share those long links without having to take away your own opinions.
Just paste your long url into the box below and click submit and we'll give you a short link to use wherever you need.
            </pre>
            </div>
            <form method="post" enctype="multipart/form-data" asp-controller="Home" asp-action="Shorten">
                <div class="input-group">
                    <input type="url" class="form-control input-lg" id="originalUrl" name="originalUrl" required pattern="https?://.+" placeholder="Start with http://... or https://..." />
                    <div class="input-group-btn">
                        <input type="submit" class="btn btn-info btn-lg offset-2" value="Shorten!" />
                    </div>
                </div>
            </form>
            @if(ViewBag.Error != null)
            {
                @ViewBag.Error
            }
            @if (ViewBag.ShortUrl != null)
            {
        <div>
            @ViewBag.LongUrl is now converted to <a href="@ViewBag.ShortUrl">@ViewBag.ShortUrl</a>
        </div>
            }
        </div>
    </div>
</div>